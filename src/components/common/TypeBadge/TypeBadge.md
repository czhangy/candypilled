# TypeBadge

A small badge image for a Pokemon or move type (e.g. `/types/fire.png`).

## Props

| Prop     | Type     | Required | Default | Description                        |
| -------- | -------- | -------- | ------- | ---------------------------------- |
| `type`   | `string` | Yes      | -       | The type whose badge is shown      |
| `width`  | `number` | Yes      | -       | The badge's rendered width, in px  |
| `height` | `number` | Yes      | -       | The badge's rendered height, in px |
