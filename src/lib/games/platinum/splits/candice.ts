import LAKE_VALOR from '@/lib/games/platinum/locations/lake-valor';
import { Split } from '@/lib/static/types';

const CANDICE: Split = { name: 'Candice', locations: [LAKE_VALOR] };

export default CANDICE;
