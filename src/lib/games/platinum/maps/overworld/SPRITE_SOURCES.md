# Overworld Sprite Sources

Tracks where each character folder's sprites were cropped from
`spritesheet.png`, so a sprite can be regenerated (re-cropped, fixed,
or have a missing direction added) without re-searching the sheet.

`spritesheet.png` is a "PKMN Platinum Spritesheet" rip (credited to
spacemotion within the image itself). It has no embedded labels —
characters are laid out in columns of flat-color background blocks (one
column per character, or one wider block shared by a male/female pair
using the same design), each column containing several rows: a down-facing
idle frame, one or two down walk frames, an up-facing idle frame, up walk
frame(s), and a left-facing idle + walk frames. There is no separate
right-facing frame anywhere on the sheet — every existing `right.png` in
this directory is `left.png` mirrored horizontally (confirmed via pixel
diff on `mars`).

Each output sprite is a 30x30 RGBA PNG: a direct pixel crop of the sheet
(no resizing) with the cell's flat background color chroma-keyed to
transparent (alpha 0).

## Known regions

| Folder             | Sheet region (top-left x,y of each 30x30 crop)                                                                                                                                                                                                        | Notes                                                                                                                                                                                                                                          |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `galactic-grunt-m` | `down`: (50, 1561) · `up`: (50, 1593) · `left`: (52, 1625) · `right`: mirrored from `left`                                                                                                                                                            | Teal/turquoise round-hair, grey blazer uniform. Male and female share the same design/colors — the sheet draws them as two adjacent columns with identical coloring, differing only by which column (male = left of the pair, female = right). |
| `galactic-grunt-f` | `down`: (82, 1561) · `up`: (82, 1593) · `left`: (82, 1625) · `right`: mirrored from `left`                                                                                                                                                            | See above.                                                                                                                                                                                                                                     |
| `mars`             | Not recorded — extracted before this file existed. Re-locate by eye if it ever needs regenerating (search for the red-haired, white-blouse Team Galactic Commander design near the "Other"/named-NPC section, roughly y > 1550 in the current sheet). |                                                                                                                                                                                                                                                |
| `barry`            | Not recorded — same caveat as `mars`.                                                                                                                                                                                                                 |                                                                                                                                                                                                                                                |
| `scientist`        | `down`: (306, 674) · `up`: (306, 706) · `left`: (307, 738) · `right`: mirrored from `left`                                                                                                                                                            | Team Galactic-style scientist: dark grey hair, glasses, white lab coat over black pants. Column background (144, 136, 128) spans x 305-337, y 673-957 (9-frame block: down idle/walk/walk, up idle/walk/walk, left idle/walk/walk).            |
| `cyrus`            | `down`: (18, 1561) · `up`: (18, 1593) · `left`: (19, 1626) · `right`: mirrored from `left`                                                                                                                                                            | Team Galactic Boss Cyrus: blue slicked-back hair, grey blazer with yellow accent. Column background (232, 216, 216) spans x 17-49, in the "Other" named-NPC section starting ~y 1561, same 9-frame block layout as `scientist`.                |
| `saturn`           | `down`: (146, 1561) · `up`: (146, 1592) · `left`: (147, 1626) · `right`: mirrored from `left`                                                                                                                                                         | Team Galactic Commander Saturn: navy blue hair styled into tall cat-ear-like points, white/grey uniform. Column background (232, 192, 248) spans x 145-176, same row block as `cyrus` (~y 1561+).                                              |
| `jupiter`          | `down`: (178, 1562) · `up`: (178, 1594) · `left`: (179, 1624) · `right`: mirrored from `left`                                                                                                                                                         | Team Galactic Commander Jupiter: pink hair in a tall top bun, white/grey uniform. Column background (128, 192, 248) spans x 177-208, same row block as `cyrus`/`saturn` (~y 1561+).                                                            |

| `youngster` | `down`: (18, 384) · `up`: (18, 416) · `left`: (19, 449) · `right`: mirrored from `left` | Blue rounded cap, red tank top, blue overalls. Column background (32, 128, 96) spans x 17-48 (x=49 is already the next character's color — keep crops ≤48). Standard 9-row block starting y392: row1(y392)=down idle, row2(y424)=up idle, row3(y456)=left idle, rows4-9=walk cycles. **A prior pass started scanning this block at y~424 instead of its true start y392, so row1 (down idle) was never found — row5 (a down walk frame) got used instead.** Always find the actual top of a 9-row block before assuming which row is row1. |
| `camper` | `down`: (82, 385) · `up`: (82, 416) · `left`: (83, 449) · `right`: mirrored from `left` | Green pith helmet with red badge, blue collar, green/grey overalls with white trim. Male half (left column) of a shared-design pair with `picnicker`. Column background (136, 96, 96) spans x 81-113 (male half of the wider 81-145 twin block), same 9-row block/idle rows as `youngster` (row1=down @ y392, row2=up @ y424, row3=left @ y456). Note the `down` crop's y0 is 385, not 384 — y384 is a 1px transition row of a different grey ((176,168,176), left over from the block above) that isn't part of this character's bg. |
| `picnicker` | `down`: (114, 385) · `up`: (114, 416) · `left`: (115, 449) · `right`: mirrored from `left` | Same design as `camper` but with visible side ponytail hair. Female half (right column) of the pair, x 113-145, background (136, 96, 96). Same row layout/idle rows as `youngster`/`camper`. Same y384-transition-row caveat as `camper`'s `down` — use y0=385. |
| `lass` | `down`: (306, 97) · `up`: (306, 129) · `left`: (307, 161) · `right`: mirrored from `left` | Olive/mustard cap with cross emblem, brown hair, blue overalls with orange trim, red collar. Column background (32, 128, 96) spans x 305-337, in the "kid trainers" section starting ~y95 (separate occurrence of this x-range from the `camper`/`picnicker`/`youngster` block below it). Standard 9-row block: row1(y103)=down idle, row2(y135)=up idle, row3(y167)=left idle, rows4-9=walk cycles. |
| `hiker` | `down`: (209, 673) · `up`: (209, 704) · `left`: (210, 737) · `right`: mirrored from `left` | Khaki wide-brim hat, full beard, khaki/tan outfit, orange arm accents. Column background (32, 128, 96) spans x 209-241, in the "adult NPC" section, standard 9-row block starting y677 (row1=down idle @ y677, row2=up idle @ y709, row3=left idle @ y741, rows4-9=walk cycles). **A prior pass wrongly started scanning this block partway through (~y800) and grabbed walk-cycle rows 5/6/8 instead of the true idle rows 1/2/3** — when locating a 9-row block, always scan from where the column's background first starts, not from wherever content is first noticed. **The `down` crop's y0 must be 673, not 672** — y672 is a 1px transition/shadow row of a different grey shade ((184,184,184), bled from the block boundary above) that the chroma key won't match, leaving a solid-color line across the top of the sprite. Always sample the exact bg pixel at your chosen y0/x0 before trusting a bbox-derived crop origin — don't assume the color is uniform right up to the block boundary. |
| `collector` | `down`: (273, 673) · `up`: (273, 704) · `left`: (275, 737) · `right`: mirrored from `left` | Brown/khaki pith cap with cross emblem, grey collar, blue overalls with strap pattern. Column background (32, 128, 96) spans x 273-305 **(304 is the last valid column — x=305 already belongs to the next character's bg color (144,136,128))**. Same row block as `hiker` (row1=down idle @ y678, row2=up idle @ y710, row3=left idle @ y742). Same `down` transition-row mistake as `hiker`, fixed the same way (y0=673). The `left` crop's x0 must be 275, not 276 — x0=276 put the crop's right edge at x=305, one column past this character's own background, producing a solid vertical stripe of the neighboring color down the right edge. |
| `ruin-maniac` | `down`: (146, 97) · `up`: (146, 129) · `left`: (147, 161) · `right`: mirrored from `left` | Khaki fedora, **beardless** young face, green vest (no beard, no grey trim — do not confuse with the bearded khaki-fedora character at x337-369, which is a different, unused character). Column background (80, 136, 176) spans x 145-177, standard 9-row block starting y103 (row1=down idle, row2=up idle, row3=left idle). A prior pass grabbed the bearded x337 column instead — that bg color happens to occur at exactly two x-ranges on the sheet (145-176 and 337-368), so matching background color alone isn't enough to confirm same character; check the actual design/reference image too. |

**General note on this sheet's row convention (this is a hard rule, not a heuristic)**: every character column is 9 rows tall (32px each). Rows 1-3, in order, are **always** the idle frames: row1=down, row2=up, row3=left. Rows 4-9 are walk-cycle frames (2 each for down/up/left) and are **never** idle, no matter how symmetric or "standing-still" a walk frame happens to look at a glance. Identify the idle rows purely by their row position (1st/2nd/3rd row of the block) — do not try to eyeball which frame "looks idle" (arm position, stance compactness, etc.) as a substitute for finding the true row 1. Two separate past mistakes came from this: (1) starting the row-scan partway into a block (e.g. y~~800 instead of the block's actual start y~~677) and grabbing walk rows 5/6/8 instead of idle rows 1/2/3 for `hiker`/`collector`; (2) for the `youngster`/`camper`/`picnicker` block specifically, the scan started at y~424 (already row2) because that's where a recognizable character was first spotted, missing that the block's true row1 was up at y392 — this produced walk frames for `down` even though the visual "arms position" of the wrong row looked plausible. **Always locate the exact top of the 9-row block first** (scan upward from any known row until the character's design changes to a different character, or the background color's context changes) before assigning row numbers.

When adding a new character, append a row here with the exact `(x, y)`
crop origin used for each pose, even if a pose was derived (e.g. `right`
mirrored from `left`) — that's what makes the next regeneration a lookup
instead of a search.
