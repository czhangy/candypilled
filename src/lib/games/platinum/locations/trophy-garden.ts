import { trophyGarden } from '@/lib/games/platinum/maps';
import { Location } from '@/lib/static/types';

const TROPHY_GARDEN: Location = {
    name: 'Trophy Garden',
    map: trophyGarden,
    encountersKey: 'trophy-garden',
};

export default TROPHY_GARDEN;
