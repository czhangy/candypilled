import { hearthomeCity } from '@/lib/games/platinum/maps';
import { Location } from '@/lib/static/types';

const HEARTHOME_CITY: Location = {
    name: 'Hearthome City',
    map: hearthomeCity,
    encountersKey: 'hearthome-city-area',
};

export default HEARTHOME_CITY;
