import { lakeVerity } from '@/lib/data/diamond-pearl/maps';
import { Location } from '@/lib/static/types';

const LAKE_VERITY: Location = {
    name: 'Lake Verity',
    map: lakeVerity,
    encountersKey: 'lake-verity',
};

export default LAKE_VERITY;
