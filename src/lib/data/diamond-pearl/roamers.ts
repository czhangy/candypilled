import { Roamer } from '@/lib/static/types';

// Shared by Diamond and Pearl (see ./diamond/diamond.ts and
// ./pearl/pearl.ts) since the roamer is identical between the two versions.
export const ROAMERS: Roamer[] = [{ species: 'mesprit', level: 50 }];
